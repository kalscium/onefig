use std::{io, path::Path};
use crate::conff::ConfFile;

fn compile(conff: ConfFile, path: &Path) -> Result<(), io::Error> {
    todo!()
}

fn decompile(path: &Path) -> Result<ConfFile, io::Error> {
    todo!()
}