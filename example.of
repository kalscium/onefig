// Testing
dave.name: "Dave Smith";
dave.age = 23;
dave.skills = [
    "programming"
    "people skills"
    "and other stuff"
];

dave.dog = {
    name: "Biscut",
    age: 5,
};

# Global varibles
12."dave".name: value;
var env;
env.nix-version: 23;
conff git nixos: "/etc/nixos/configuration.nix";

// Stuff for nix os
# Yoo both kinds of comments work

nixos >> {
    version: (env.VERSION);
}

nixos$ sudo nixos-rebuild switch --upgrade;